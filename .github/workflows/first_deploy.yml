name: Your Workflow Name

on:
  push:
    branches: [ main ]  # Change branch name if needed

jobs:
  your_job_name:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: ${{secrets.RCM_DB_SERVICE_ACCOUNT}}
          # Optional: Customize project_id, create_credentials_file, etc.

      - name: Your Deployment Steps...
        # Your code to deploy the SQL script using gcloud or other tools
